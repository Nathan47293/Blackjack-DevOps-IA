apiVersion: v1
kind: ConfigMap
metadata:
  name: blackjack-config
  labels:
    app: blackjack
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "info"
---
apiVersion: v1
kind: Secret
metadata:
  name: blackjack-secrets
  labels:
    app: blackjack
type: Opaque
data:
  # Example secrets - base64 encoded
  # Use: echo -n "your-secret" | base64
  api-key: ""  # Add your actual base64 encoded secrets here